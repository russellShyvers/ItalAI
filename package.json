{
  "scripts": {
    "lighthouse": "lhci autorun && node scripts/compare-lighthouse.js",
    "lighthouse:manual": "lhci collect --url=http://localhost:4000",
    "lighthouse:compare": "node scripts/compare-lighthouse.js"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1"
  }
}